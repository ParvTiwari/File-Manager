# File-Manager
A simple command-line File Manager built in C using low-level system calls. This project demonstrates core file handling operations in a Unix/Linux environment without relying on high-level libraries.
